@model IEnumerable<JamesRecipes.Models.Tip>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_FELayout.cshtml";
}
<h2 class="card-title fw-semibold mb-4">Recipe Management</h2>

<table class="table ">
    <thead>
    <tr>
        <th>
            Id
        </th>
        <th>
            Category
        </th>
        <th>
            Name
        </th>
        <th>
            Posted By
        </th>
        <th>
            Date Post
        </th>
        <th>
            Action
        </th>
    </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryTip.CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User!.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                <a asp-action="GetRecipe" asp-controller="RecipeManagement" asp-route-id="@item.TipId">Detail</a> 
            </td>
        </tr>
    }
    </tbody>
</table>